export const QUEUE_NAMES = { DEFAULT: 'default' } as const;
export const JOB_NAMES = {
  SEND_EMAIL: 'send-email',
  SEND_QUOTATION_EMAIL: 'send-quotation-email',
  GENERATE_SUBSCRIPTION_INVOICES: 'generate-subscription-invoices',
} as const;
